// Números:
let comprimento = 16;
let peso = 7.5;

// Strings:
let cor = 'Vermelho';
let sobreNome = 'Johnson';

// Booleans:
let maiorDeIdade = true;
let tanqueCheio = false;

// Object:
const produto = { descricao: 'Galaxy S22', fabricnte: 'Samsung', preco: 4200.00 };

// Array object:
const clientesVip = ['Fernanda Campos', 'Marinalva Dias', 'Michele Borges', 'Cristiano Silva'];

// Date object:
const inicioNovoAno = new Date('01/01/2023');

console.log(inicioNovoAno);
console.log(inicioNovoAno instanceof Date);
